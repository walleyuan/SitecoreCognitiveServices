@model Microsoft.ProjectOxford.Vision.Contract.Category[]

@if (Model != null)
{
    <div>
        <h3>Categories</h3>
        @foreach (var c in Model)
        {
            <div>
                <div>
                    <label>@c.Name</label>
                    <span>@c.Score</span>
                </div>
            </div>
        }
    </div>
}